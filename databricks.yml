bundle:
  name: mlops-blueprint

artifacts:
  default:
    type: whl
    path: .
    build: uv build

include:
  - resources/*.yml

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://dbc-c01c01f3-1172.cloud.databricks.com
      profile: "DEFAULT"

  prod:
    mode: production
    workspace:
      host: https://dbc-c01c01f3-1172.cloud.databricks.com
      profile: "DEFAULT"
